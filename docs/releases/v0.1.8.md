# QuotaDock v0.1.8

## 修复同步时间时区

- 修复 OpenCode Go 显示 UTC 时间的问题：例如数据中的 `13:22Z` 现在会按 Windows 本地时区显示为北京时间 `21:22`。
- 统一处理 JSON 反序列化后的 `DateTime`、`DateTimeOffset` 和带 `Z` 的 ISO 字符串。
- 同步年龄和“数据已过期”判断也使用同一套时区安全解析，不再只修正文字显示。
- 保留 v0.1.7 的绝对重置时间与动态倒计时。
