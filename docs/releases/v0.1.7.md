# QuotaDock v0.1.7

## OpenCode Go 同步显示修复

- 后台每轮成功抓取后保存绝对重置时间，不再只保存一次性的“约 X 分钟后重置”文本。
- 浮窗每 5 秒重新计算剩余时间，显示动态倒计时。
- 最近成功同步时间显示到秒，便于确认后台轮询是否继续工作。
- 浏览器桥接与后台直连都保留重置元数据；浏览器桥接仍然是备用来源。
- UI 标注为“轮询同步”，明确它是约每 60 秒抓取一次，而不是官方实时推送。

## 兼容与隐私

升级包不包含 OpenCode Cookie、凭证文件或本机额度数据。已有本地凭证和额度文件会保留，升级只替换程序脚本。
